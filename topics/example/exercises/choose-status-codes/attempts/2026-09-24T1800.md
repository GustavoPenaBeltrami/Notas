1. 404 — the client asked for an order that doesn't exist.
2. 201 — the server created the order the client sent.
3. 503 — the server can't serve right now; the failure is temporary and on its side.
4. 301 — the order moved permanently, so the client should use the new URL.
